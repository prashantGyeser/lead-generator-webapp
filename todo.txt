1. Setup validations in the setup form.
    Make this live validation so as soon as a field looses focus, they user is told if they have made a mistake or not
       The user needs to enter atleast one keyword
       The city is mandatory.
1 a. Create the styling for the validation errors
3. Convert a city into a lat long before it is stored - Done, test
4. Add a proper message for the initial modal window that is shown when a user first sees their leads page


